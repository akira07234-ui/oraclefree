# 上线操作手册 — bazioracle

目标：把 `site/` 部署到 Cloudflare Pages，得到 `https://bazioracle.pages.dev`，
并在上线当天完成 SEO 基础配置。全程约 40 分钟。

> 已确认的方案（2026-08-31）：先用免费 pages.dev 域名，以后做起来了再买自定义域名。

---

## 开始前的两个常识

1. **项目名 = 域名**。Pages 项目名填 `bazioracle`，域名就是 `bazioracle.pages.dev`；
   如果名字被占用改成了别的，域名跟着变，后面第 0 步要用新地址重来（3 分钟的事）。
2. **canonical/sitemap 现在指向占位域名** `www.bazioracle.com`。
   不改直接上线，Google 会认为"真身"在一个不存在的域名上，SEO 从第一天就是错的。
   所以第 0 步必须做。

---

## 第 0 步 · 用最终域名重新生成站点（5 分钟，必做）

在 `bazioracle/build` 目录执行（两种终端任选其一）：

**Git Bash：**
```bash
cd bazioracle/build
SITE_URL=https://bazioracle.pages.dev node build.js
```

**PowerShell：**
```powershell
cd bazioracle\build
$env:SITE_URL = "https://bazioracle.pages.dev"
node build.js
```

然后跑回归（应显示 755 项断言通过 / 紫微全部通过）：
```bash
node test-wuxing.js
node test-ziwei.js
```

**验证域名已换干净：**
```bash
grep -o "https://[a-z.]*" ../site/sitemap.xml | sort -u
```
只应出现 `https://bazioracle.pages.dev`，不能再有 `bazioracle.com`。

---

## 第 1 步 · 注册 Cloudflare（5 分钟）

1. 打开 https://dash.cloudflare.com → Sign up（只要一个邮箱，免费，不用绑卡）
2. 邮箱验证后进入控制台

---

## 第 2 步 · 创建 Pages 项目并上传（10 分钟）

1. 左侧菜单 **Workers & Pages → Create → Pages 标签 → Upload assets**
2. **Project name 填 `bazioracle`** —— 这一步就是定域名，页面上会显示
   将得到的完整地址，确认是 `bazioracle.pages.dev`
3. 把整个 **`site/` 文件夹直接拖进**上传框（不是 zip，拖文件夹）
   - 体量：1002 个文件 / 38MB，全部远低于限制（单文件 25MB / 2 万文件）
4. 点 **Deploy site**，等 1-3 分钟

⚠️ 最容易踩的坑：如果你在第 2 步发现项目名被占用而改了名，
**回到第 0 步用新地址重新 build**，再回来上传。

---

## 第 3 步 · 上线验证清单（15 分钟，逐项打勾）

访问 `https://bazioracle.pages.dev`：

- [ ] `/` 打开是英文首页，无样式错乱
- [ ] `/zh/` 中文站，右上五语言切换器能跳且语言正确
- [ ] `/zh/bazi/` 排盘：1991-07-23 05:05 男 → 辛未 乙未 甲午 丁卯
- [ ] `/zh/ziwei/` 紫微：大限年龄范围、当前大限金框高亮、切换年份正常
- [ ] `/zh/five-elements/` 五行：六段式板块齐全，木显示「偏弱」+ 月令徽标「囚」
- [ ] 页面源码里 canonical 是 `https://bazioracle.pages.dev/...`
- [ ] `https://bazioracle.pages.dev/sitemap.xml` 能打开
- [ ] `/ar/` 阿语页从右到左显示正常
- [ ] **手机打开** `/zh/` 和排盘各试一遍（大部分流量会是移动端）

---

## 第 4 步 · Google Search Console（上线当天做，15 分钟）

1. 打开 https://search.google.com/search-console → **添加资源**
2. 选 **「网址前缀」**（不是"网域"——pages.dev 没法做 DNS 验证）
   填 `https://bazioracle.pages.dev`
3. 验证方式选 **「HTML 文件」**：下载 `googleXXXX.html` → 放进 `bazioracle/site/` 根目录
   → 回到 Pages 项目 **Create new deployment** 再传一次 site/ → 回 GSC 点验证
4. 验证通过后：左侧 **站点地图** → 输入 `sitemap.xml` → 提交
5. 用顶部的 **网址检查** 分别检查 `https://bazioracle.pages.dev/` 和 `/zh/`，
   点「请求编入索引」——加速首日收录

---

## 第 5 步 · 流量统计（可选，5 分钟）

Dashboard → **Web Analytics → Enable** —— Cloudflare 会自动向所有页面注入统计脚本，
**不用改任何代码**，次日就能看到 PV/访客/国家分布。

---

## 以后每次更新内容

```bash
cd bazioracle/build
node build.js          # 改了 build/ 里的内容或语言包后
node test-wuxing.js    # 回归
node test-ziwei.js
```

然后上传：Pages 项目页 → **Create new deployment** → 再拖一次 `site/`。
旧版本自动保留，出问题可一键回滚（Deployments 里选上一个版本 → Rollback）。

---

## 进阶 · 命令行部署（可选，装了 Node 就行）

网页拖拽做两三次后如果嫌烦，换命令行，以后更新只剩一条命令：

```bash
npm install -g wrangler
wrangler login              # 弹浏览器授权，点 Allow
cd bazioracle
npx wrangler pages deploy site --project-name=bazioracle
```

---

## 以后买自定义域名（有流量了再弄，30 分钟）

1. 买域名：Cloudflare Registrar（成本价无加价）或 Namecheap，.com 约 60-100 元/年
2. Pages 项目 → **Custom domains → Set up a custom domain** → 按提示加 CNAME
3. **重新生成**：`SITE_URL=https://你的域名 node build.js` → 重新上传
4. GSC 的网址前缀资源换成新域名重新添加 + 提交 sitemap
5. pages.dev 旧地址继续可访问，Google 会随新 canonical 逐步迁移，不用做跳转

---

## 别忘了 / 别做

- ✅ **隐私承诺**：全站排盘纯前端计算、生日不上传。别往站里加任何收集用户数据的脚本，
  这是隐私政策页白纸黑字写过的
- ⏸️ **AdSense 先别碰**：`ads.txt` 还是占位符。新站 2-4 周有真实流量后再申请，通过率高得多
- ⏸️ 掷筊的"伪随机"已标注为娱乐；想换真随机源属于后续优化，不阻塞上线
